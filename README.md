# B-loved Player

Prototype Angular + Electron player (turntable style) — MVP pour mariage.

Démarrage (après `npm install`):

```bash
npm run start
```

Electron (dev):

```bash
npm run electron:serve
```
